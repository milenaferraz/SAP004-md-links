import mdLinks from '../index.js';
import readFile from '../src/readFile';


describe('mdLinks', () => {

  it('should return 5', () => {
    expect(5).toBe(5);
    
  });

});



describe('readFile', () => {

  it('should return 5', () => {
    expect(5).toBe(5);
    
  });

});